-- BSHK_DATテーブルの列サイズ変更
ALTER TABLE BSHK_DAT
ALTER COLUMN BSHK_TO decimal(3);

-- BRCP_DATテーブルの列サイズ変更
ALTER TABLE BRCP_DAT
ALTER COLUMN BRCP_ORDER_CMP decimal(3);

-- 以下は先ほどのUPDATE文
-- BSHK_DATテーブルの出荷先ID更新（加工宛）
UPDATE BSHK_DAT
SET BSHK_TO = 601
WHERE BSHK_TO = 1;

-- BRCP_DATテーブルの発注元ID更新（コロンバス）
UPDATE BRCP_DAT
SET BRCP_ORDER_CMP = 501
WHERE BRCP_ORDER_CMP = 2;

-- BSHK_DATテーブルの出荷先ID更新（加工宛）
    UPDATE BSHK_DAT
    SET BSHK_TO = 501
    WHERE BSHK_TO = 2;

-- BRCP_DATテーブルの発注元ID更新（ダラス）
UPDATE BRCP_DAT
SET BRCP_ORDER_CMP = 502
WHERE BRCP_ORDER_CMP = 3;

-- BSHK_DATテーブルの出荷先ID更新（加工宛）
    UPDATE BSHK_DAT
    SET BSHK_TO = 502
    WHERE BSHK_TO = 3;

-- BRCP_DATテーブルの発注元ID更新（ヤンガー）
UPDATE BRCP_DAT
SET BRCP_ORDER_CMP = 503
WHERE BRCP_ORDER_CMP = 4;

-- BSHK_DATテーブルの出荷先ID更新（加工宛）
    UPDATE BSHK_DAT
    SET BSHK_TO = 503
    WHERE BSHK_TO = 4;

-- BRCP_DATテーブルの発注元ID更新（ヤンガーヨーロッパ）
UPDATE BRCP_DAT
SET BRCP_ORDER_CMP = 504
WHERE BRCP_ORDER_CMP = 5;

-- BSHK_DATテーブルの出荷先ID更新（加工宛）
    UPDATE BSHK_DAT
    SET BSHK_TO = 504
    WHERE BSHK_TO = 5;

-- BSHK_DATテーブルの出荷先ID更新（加工宛）
    UPDATE BSHK_DAT
    SET BSHK_TO = 999
    WHERE BSHK_TO = 9;
